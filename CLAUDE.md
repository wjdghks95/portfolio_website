# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Portfolio website project (`포트폴리오 웹사이트 by Claude`). Currently in early setup phase — configured as a Java project via IntelliJ IDEA, with `src/` as the source root.

## Project Structure

- `src/` — Java source files (currently empty)
- `portfolio_website.iml` — IntelliJ IDEA module config (Java module, inherits JDK)
- `.idea/` — IDE project metadata

## Notes

- No build system (Maven/Gradle) has been configured yet. When one is added, update this file with build, run, and test commands.
- The `.gitignore` is set up for Java/IntelliJ development (ignores `out/`, `bin/`, `.classpath`, `.project`, etc.).
